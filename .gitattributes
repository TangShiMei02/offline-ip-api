# 统一换行符为 LF。
#
# 原因：这个项目主要在 Windows 上开发，但部署目标是 Linux。
# 如果 shell 脚本带 CRLF 换行，Linux 上会报 `\r: command not found` / `set: pipefail: invalid option`
# 之类的怪错 —— 排查成本很高，而且 git 默认的 core.autocrlf 在不同机器上行为不一致。
# 这里显式声明，任何人 clone 下来都不会踩到这个坑。

* text=auto eol=lf

# 明确按文本处理（必须 LF）
*.sh      text eol=lf
*.service text eol=lf
*.py      text eol=lf
*.md      text eol=lf
*.conf    text eol=lf
*.example text eol=lf
*.html    text eol=lf
*.css     text eol=lf
*.js      text eol=lf
Dockerfile text eol=lf
.gitignore text eol=lf

# 二进制文件：不要做任何换行符转换
*.ico  binary
*.xdb  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary

# 打包/压缩产物在 diff 里没有意义
*.zip  binary
